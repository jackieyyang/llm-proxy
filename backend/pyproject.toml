[project]
name = "llm-proxy-backend"
version = "0.1.0"
description = "Backend service for LLM Proxy"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.127.0",
    "httpx>=0.28.1",
    "loguru>=0.7.3",
]
